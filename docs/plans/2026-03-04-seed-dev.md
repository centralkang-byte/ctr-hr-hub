# seed-dev.ts Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** CTR-KR(한국)/CTR-CN(중국) 중심의 풍부한 개발용 시드 데이터 생성 (`prisma/seed-dev.ts`)

**Architecture:** 기존 seed.ts(마스터)가 먼저 실행된 후 이 시드가 실행됨. company/dept/grade/policy를 DB에서 조회하여 참조. deterministicUUID 기반 upsert로 멱등성 보장.

**Tech Stack:** TypeScript, Prisma v7, PrismaPg adapter, tsx

---

## 데이터 목표 (최종)

### CTR-KR
- 부서 계층: 3개 사업본부 > 6개 부문 > 12개 팀 (총 21개)
- 직원: 40명 (부서별 2~5명, 직급 분포 현실적)
- 근태: 최근 3개월 일자별 (정상/지각/조퇴/결근 혼합)
- 휴가: 정책 7종 + 잔여량 + 신청 25건 (PENDING/APPROVED/REJECTED)
- 교대: 2조2교대 패턴, 생산직 10명 배정, 이번달+다음달 스케줄
- 공휴일: 2025~2026 (한국, 이미 있음 → skip)
- 단말기: 3대
- 온보딩: 진행 중 3건, 완료 2건
- 오프보딩: 진행 중 2건
- 성과사이클: 2025-H2(CLOSED), 2026-H1(EVAL_OPEN)
- MBO 목표: 직원당 3개, 달성점수 포함
- 성과평가: SELF + MANAGER 평가 (SUBMITTED/CONFIRMED)
- 역량평가: 점수 + EMS블록
- 1:1 미팅: 매니저별 5건씩
- 인정(Recognition): 15건
- 캘리브레이션: 세션 1개 + 조정 내역
- 표창 2건, 징계 1건

### CTR-CN
- 부서 계층: 2개 본부 > 5개 팀 (총 7개)
- 직원: 20명
- 근태: 최근 1개월
- 휴가: 정책 5종 + 잔여 + 신청 15건
- 공휴일: 2025~2026 중국 법정공휴일

---

## Task 1: 파일 생성 및 보일러플레이트

**Files:**
- Create: `prisma/seed-dev.ts`
- Modify: `package.json` (seed:dev 스크립트 추가)

**Step 1: package.json에 seed:dev 추가**

`package.json`의 `prisma.seed` 아래에 추가:
```json
"scripts": {
  "seed:dev": "npx tsx prisma/seed-dev.ts"
}
```

**Step 2: seed-dev.ts 헤더 작성**

```typescript
/**
 * CTR HR Hub v3.2 — Dev Seed (테스트용 트랜잭션 데이터)
 * 대상: CTR-KR, CTR-CN
 * 전제: prisma db seed (마스터 시드) 실행 완료 후 실행
 * Usage: npm run seed:dev
 * Idempotent: deterministicUUID + upsert 기반
 */
import dotenv from 'dotenv'
import path from 'path'
dotenv.config({ path: path.resolve(__dirname, '..', '.env.local') })
dotenv.config({ path: path.resolve(__dirname, '..', '.env') })

import { PrismaClient } from '../src/generated/prisma/client'
import { PrismaPg } from '@prisma/adapter-pg'

const DATABASE_URL = process.env.DATABASE_URL
if (!DATABASE_URL) throw new Error('DATABASE_URL is not set')

const adapter = new PrismaPg({ connectionString: DATABASE_URL })
const prisma: PrismaClient = new (PrismaClient as any)({ adapter, log: ['warn', 'error'] })

function deterministicUUID(namespace: string, key: string): string {
  const str = `${namespace}:${key}`
  let hash = 0
  for (let i = 0; i < str.length; i++) {
    const chr = str.charCodeAt(i)
    hash = ((hash << 5) - hash) + chr
    hash |= 0
  }
  const hex = Math.abs(hash).toString(16).padStart(8, '0')
  return `${hex.slice(0, 8)}-${hex.slice(0, 4)}-4${hex.slice(1, 4)}-a${hex.slice(0, 3)}-${hex.padEnd(12, '0').slice(0, 12)}`
}

// 날짜 헬퍼
function daysAgo(n: number): Date {
  const d = new Date(); d.setDate(d.getDate() - n); d.setHours(0,0,0,0); return d
}
function dateAt(d: Date, h: number, m = 0): Date {
  return new Date(d.getFullYear(), d.getMonth(), d.getDate(), h, m)
}
```

---

## Task 2: CTR-KR 부서 계층 구조

**목표 구조:**
```
경영지원본부 (MGMT, L1)
  ├── 인사팀 (HR, L2)  ← 이미 있음
  ├── 재무/회계팀 (FIN, L1) → L2로 이동
  └── 법무팀 (LEGAL, L2)
생산사업본부 (MFG_HQ, L1)
  ├── 생산/제조팀 (MFG, L2) ← 이미 있음
  ├── 품질관리팀 (QA, L2) ← 이미 있음
  └── 구매/조달팀 (PUR, L2) ← 이미 있음
기술연구본부 (TECH_HQ, L1)
  ├── 연구개발팀 (RANDD, L2) ← 이미 있음
  └── 개발팀 (DEV, L2) ← 이미 있음
영업마케팅본부 (SALES_HQ, L1)
  └── 영업팀 (SALES, L2) ← 이미 있음
  └── 마케팅팀 (MKT, L2)
```

**Step 1: DB에서 기존 부서 ID 조회 후 신규 부서 upsert**

```typescript
// DB에서 기존 부서 조회
const existingDepts = await prisma.department.findMany({
  where: { companyId: krId },
  select: { id: true, code: true }
})
const deptMap: Record<string, string> = {}
existingDepts.forEach(d => { deptMap[d.code] = d.id })

// L1 본부 생성
const l1Depts = [
  { code: 'MFG_HQ', name: '생산사업본부', nameEn: 'Manufacturing Division', level: 1, sortOrder: 1 },
  { code: 'TECH_HQ', name: '기술연구본부', nameEn: 'Technology & R&D Division', level: 1, sortOrder: 2 },
  { code: 'SALES_HQ', name: '영업마케팅본부', nameEn: 'Sales & Marketing Division', level: 1, sortOrder: 3 },
]
// MGMT은 이미 있음 → parentId 업데이트만
for (const d of l1Depts) {
  const id = deterministicUUID('dept-kr', d.code)
  await prisma.department.upsert({
    where: { companyId_code: { companyId: krId, code: d.code } },
    update: { name: d.name, nameEn: d.nameEn, level: d.level, sortOrder: d.sortOrder },
    create: { id, companyId: krId, code: d.code, name: d.name, nameEn: d.nameEn, level: d.level, sortOrder: d.sortOrder },
  })
  deptMap[d.code] = id
}

// 기존 L1 부서들에 parentId 설정
const parentMapping: Record<string, string> = {
  MFG: 'MFG_HQ', QA: 'MFG_HQ', PUR: 'MFG_HQ',
  RANDD: 'TECH_HQ', DEV: 'TECH_HQ',
  SALES: 'SALES_HQ',
  HR: 'MGMT', FIN: 'MGMT',
}
for (const [childCode, parentCode] of Object.entries(parentMapping)) {
  if (deptMap[childCode] && deptMap[parentCode]) {
    await prisma.department.update({
      where: { id: deptMap[childCode] },
      data: { parentId: deptMap[parentCode], level: 2 }
    })
  }
}

// 신규 L2 부서
const newL2Depts = [
  { code: 'LEGAL', name: '법무팀', nameEn: 'Legal Team', parentCode: 'MGMT', sortOrder: 3 },
  { code: 'MKT', name: '마케팅팀', nameEn: 'Marketing Team', parentCode: 'SALES_HQ', sortOrder: 2 },
]
for (const d of newL2Depts) {
  const id = deterministicUUID('dept-kr', d.code)
  await prisma.department.upsert({
    where: { companyId_code: { companyId: krId, code: d.code } },
    update: { name: d.name },
    create: { id, companyId: krId, code: d.code, name: d.name, nameEn: d.nameEn, level: 2, sortOrder: d.sortOrder, parentId: deptMap[d.parentCode] },
  })
  deptMap[d.code] = id
}
```

---

## Task 3: CTR-CN 부서 계층 구조

**목표:**
```
생산본부 (CN_MFG_HQ, L1)
  ├── 생산팀 (CN_MFG, L2)
  └── 품질팀 (CN_QA, L2)
경영지원본부 (CN_MGMT, L1)
  ├── 인사행정팀 (CN_HR, L2)
  └── 재무팀 (CN_FIN, L2)
기술팀 (CN_TECH, L2, standalone)
```

---

## Task 4: CTR-KR 직원 40명 생성

**직원 데이터 구조:**
```typescript
interface EmpDef {
  no: string        // EMP-KR-001
  name: string      // 홍길동
  nameEn: string
  email: string     // gildong.hong@ctr.co.kr
  dept: string      // dept code
  grade: string     // G1~G6
  hireDate: Date
  role: 'HR_ADMIN' | 'MANAGER' | 'EMPLOYEE'
}
```

**부서별 직원 배분:**
- MGMT본부: 임원 1(G1) + 부장 1(G2)
- HR팀: 과장 1(G4) + 대리 1(G5) + 사원 1(G6)  ← hr@ctr.co.kr 기존 유지
- FIN팀: 과장 1 + 대리 1 + 사원 2
- LEGAL팀: 차장 1 + 사원 1
- MFG_HQ: 부장 1
- MFG팀: 차장 1 + 과장 2 + 대리 3 + 사원 3 (생산직)
- QA팀: 과장 1 + 대리 1 + 사원 2
- PUR팀: 과장 1 + 대리 1 + 사원 1
- TECH_HQ: 부장 1
- RANDD팀: 차장 1 + 과장 1 + 대리 2 + 사원 1
- DEV팀: 차장 1(manager@) + 과장 1 + 대리 1 + 사원 1
- SALES_HQ: 부장 1
- SALES팀: 과장 2 + 대리 2 + 사원 2 (employee@ 포함)
- MKT팀: 대리 1 + 사원 1

**중요:** 기존 4개 계정(admin@, hr@, manager@, employee@)은 EmployeeAssignment의 부서를 업데이트만 함.

**Step 1: 직원 upsert + EmployeeAssignment + EmployeeAuth + EmployeeRole**

이미 존재하는 직원 (hr@, manager@, employee@)의 assignment는 생성하지 않고 부서 update만.

```typescript
// 비밀번호: Test1234! (bcrypt hash)
const TEST_PWD = '$2a$10$...' // 기존 seed.ts와 동일한 해시 사용
```

---

## Task 5: CTR-CN 직원 20명 생성

CN 부서별 배분:
- CN_MFG팀: 5명 (생산직)
- CN_QA팀: 3명
- CN_MGMT: 2명
- CN_HR팀: 3명
- CN_FIN팀: 3명
- CN_TECH팀: 4명

---

## Task 6: 휴가 정책 + 잔여량 (KR, CN)

**CTR-KR 휴가 정책 (LeavePolicy):**
```typescript
const krLeavePolicies = [
  { type: 'ANNUAL',       name: '연차',         days: 15, isPaid: true,  carryOver: true,  maxCarry: 5 },
  { type: 'SICK',         name: '병가',         days: 10, isPaid: false, carryOver: false },
  { type: 'MATERNITY',    name: '출산휴가',     days: 90, isPaid: true,  carryOver: false },
  { type: 'PATERNITY',    name: '배우자출산휴가', days: 10, isPaid: true, carryOver: false },
  { type: 'BEREAVEMENT',  name: '경조사휴가',   days: 5,  isPaid: true,  carryOver: false },
  { type: 'SPECIAL',      name: '특별휴가',     days: 3,  isPaid: true,  carryOver: false },
  { type: 'COMPENSATORY', name: '대체휴가',     days: 0,  isPaid: true,  carryOver: false },
]
```

**EmployeeLeaveBalance (2026년):**
- 연차: 직원별 hireDate 기준 계산 (1년 미만 11일, 1년 이상 15일)
- usedDays: 2~8일 (랜덤하게 분산)
- pendingDays: 신청 대기 중인 날짜

**LeaveRequest (25건):**
- APPROVED: 15건 (과거 날짜)
- PENDING: 7건 (미래 날짜)
- REJECTED: 3건

---

## Task 7: 근태 단말기 + 근무 스케줄

**AttendanceTerminal (3대):**
```typescript
[
  { code: 'T-HQ-MAIN', type: 'FACE_RECOGNITION', location: '본사 정문' },
  { code: 'T-FACTORY', type: 'FINGERPRINT',       location: '공장 입구' },
  { code: 'T-HQ-BACK', type: 'CARD_READER',       location: '본사 후문' },
]
```

**WorkSchedule (CTR-KR):**
```typescript
// 표준 근무 (사무직)
{ name: '표준근무', scheduleType: 'STANDARD', weeklyHours: 40,
  dailyConfig: { mon: {start:'09:00', end:'18:00', break:60}, ... } }

// 교대근무 (생산직)
{ name: '2조2교대', scheduleType: 'SHIFT_2', weeklyHours: 40, ... }
```

**EmployeeSchedule:** 사무직은 표준, 생산직은 교대

---

## Task 8: 교대근무 패턴 + 조 + 스케줄

**ShiftPattern:**
```typescript
{
  code: 'KR-2S2',
  name: '2조2교대',
  patternType: 'TWO_SHIFT',
  cycleDays: 2,
  slots: [
    { name: '주간', start: '07:00', end: '19:00', breakMin: 60, nightPremium: false },
    { name: '야간', start: '19:00', end: '07:00', breakMin: 60, nightPremium: true },
  ]
}
```

**ShiftGroup:** A조(주간 시작), B조(야간 시작)
**ShiftGroupMember:** MFG팀 생산직 10명을 A조 5명, B조 5명으로 배정
**ShiftSchedule:** 이번달(3월) + 다음달(4월) 60일치 스케줄 생성

---

## Task 9: 근태 기록 (Attendance)

**CTR-KR 최근 3개월 (2025-12 ~ 2026-02):**

평일 기준 일자별 직원 상태:
- 정상(PRESENT): 90% → clockIn 08:50~09:05, clockOut 18:00~19:30
- 지각(LATE): 5% → clockIn 09:15~10:00
- 조퇴(EARLY_LEAVE): 2% → clockOut 15:00~17:00
- 결근(ABSENT): 1% → clockIn/Out null
- 휴가(ON_LEAVE): 승인된 LeaveRequest와 연동

```typescript
// 결정적 랜덤 (deterministic pseudo-random)
function pseudoRandom(seed: number): number {
  return ((seed * 1103515245 + 12345) & 0x7fffffff) / 0x7fffffff
}
```

**CTR-CN 최근 1개월 (2026-02):**
- 동일 방식

---

## Task 10: 중국 공휴일

**2025~2026 중국 법정공휴일 (춘절 포함):**
```typescript
const cnHolidays2025 = [
  { name: '元旦 (신정)', date: '2025-01-01' },
  { name: '春节 (춘절)', date: '2025-01-29' },
  // ... 7일 연속
  { name: '清明节 (청명절)', date: '2025-04-05' },
  { name: '劳动节 (노동절)', date: '2025-05-01' },
  { name: '端午节 (단오절)', date: '2025-05-31' },
  { name: '中秋节 (중추절)', date: '2025-10-06' },
  { name: '国庆节 (국경절)', date: '2025-10-01' },
  // 2026
  { name: '元旦 (신정)', date: '2026-01-01' },
  { name: '春节 (춘절)', date: '2026-02-17' },
  // ...
]
```

---

## Task 11: 온보딩/오프보딩 진행 건

**EmployeeOnboarding (KR):**
```typescript
// 진행 중 3건: 2월 입사자
// 완료 2건: 작년 11월 입사자
```

**EmployeeOffboarding (KR):**
```typescript
// 퇴직처리 진행 중 2건 (자진퇴사 1건, 권고퇴직 1건)
```

---

## Task 12: 성과 사이클 + MBO 목표

**PerformanceCycle:**
```typescript
[
  {
    name: '2025년 하반기 성과평가',
    year: 2025, half: 'H2',
    goalStart: '2025-07-01', goalEnd: '2025-07-31',
    evalStart: '2025-12-01', evalEnd: '2025-12-31',
    status: 'CLOSED',
  },
  {
    name: '2026년 상반기 성과평가',
    year: 2026, half: 'H1',
    goalStart: '2026-01-02', goalEnd: '2026-01-31',
    evalStart: '2026-06-01', evalEnd: '2026-06-30',
    status: 'EVAL_OPEN',
  }
]
```

**MboGoal:** 직원당 3개 목표
- 2025-H2 사이클: status=APPROVED, achievementScore 있음
- 2026-H1 사이클: status=APPROVED, achievementScore 없음 (평가 진행 중)

---

## Task 13: 성과 평가 (PerformanceEvaluation)

**2025-H2 (CLOSED 사이클):**
- 각 직원: SELF 평가 (SUBMITTED) + MANAGER 평가 (CONFIRMED)
- performanceScore: 2.0~5.0, competencyScore: 2.0~5.0
- emsBlock: A1~C3 분포

**2026-H1 (EVAL_OPEN 사이클):**
- 각 직원: SELF 평가 (DRAFT or SUBMITTED)
- MANAGER 평가는 아직 없음

---

## Task 14: 1:1 미팅 + Recognition

**OneOnOne:** MANAGER 역할 직원 1명당 부하 5명, 최근 3개월 월 1회씩
- COMPLETED: 과거 건
- SCHEDULED: 미래 건

**Recognition (CTR-KR, 15건):**
- senderId: 매니저 or 동료
- receiverId: 일반 직원
- coreValue: '혁신', '협력', '도전', '책임', '고객중심'

---

## Task 15: 캘리브레이션 세션

**CalibrationSession (2025-H2):**
```typescript
{
  name: '2025 하반기 성과 캘리브레이션',
  status: 'CALIBRATION_COMPLETED',
  blockDistribution: { A1: 5, A2: 10, A3: 5, B1: 10, B2: 35, B3: 15, C1: 5, C2: 10, C3: 5 }
}
```

**CalibrationAdjustment:** 블록 이동이 있었던 5건 기록

---

## Task 16: 표창/징계

**RewardRecord (2건):**
```typescript
[
  { type: 'COMMENDATION', reason: '2025년 우수사원 선정', amount: 500000 },
  { type: 'TEAM_AWARD',   reason: '분기 최우수팀 수상' },
]
```

**DisciplinaryAction (1건):**
```typescript
{
  type: 'WARNING', category: 'ATTENDANCE',
  description: '무단결근 3회 반복',
  actionDate: '2025-11-15', status: 'ACTIVE'
}
```

---

## Task 17: 실행 검증

**Step 1:** `npm run seed:dev` 실행
**Step 2:** 출력에서 각 섹션 ✅ 확인
**Step 3:** 앱 기동 후 각 메뉴 접속 확인:
- `/employees` → 40명 목록
- `/org` → 계층 조직도
- `/attendance` → 근태 기록
- `/leave` → 휴가 신청 목록
- `/performance` → 사이클/목표/평가
- `/onboarding` → 진행 건
